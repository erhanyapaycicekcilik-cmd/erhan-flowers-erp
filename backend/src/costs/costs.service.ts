import { BadRequestException, Injectable, NotFoundException } from '@nestjs/common';import { Prisma, RecipeCostType } from '../generated/prisma-client';import { PrismaService } from '../prisma/prisma.service';type RecipePayload = {  shopMarginPercent?: number;  siteMarginPercent?: number;  marketplaceMarginPercent?: number;  items?: Array<{    stockCardId?: number;    quantity?: number;    unit?: string;  }>;  extraCosts?: Array<{    type?: RecipeCostType;    name?: string;    amount?: number;  }>;};@Injectable()export class CostsService {  constructor(private readonly prisma: PrismaService) {}  async listProductCosts() {    const products = await this.prisma.product.findMany({      include: {        category: true,        recipe: {          include: {            items: { include: { stockCard: true } },            extraCosts: true,          },        },      },      orderBy: { createdAt: 'desc' },    });    return products.map((product) => ({      id: product.id,      productName: product.productName,      modelCode: product.modelCode,      category: product.category,      status: product.status,      costs: product.recipe ? this.calculate(product.recipe) : this.emptyCalculation(),    }));  }  async getProductRecipe(productId: number) {    const product = await this.prisma.product.findUnique({      where: { id: productId },      include: {        category: true,        recipe: {          include: {            items: {              include: { stockCard: true },              orderBy: { id: 'asc' },            },            extraCosts: {              orderBy: { id: 'asc' },            },          },        },      },    });    if (!product) {      throw new NotFoundException('Ürün bulunamadı.');    }    return {      product,      recipe: product.recipe ? this.serializeRecipe(product.recipe) : null,      costs: product.recipe ? this.calculate(product.recipe) : this.emptyCalculation(),    };  }  async saveRecipe(productId: number, payload: unknown) {    const data = this.normalize(payload);    const product = await this.prisma.product.findUnique({ where: { id: productId } });    if (!product) {      throw new NotFoundException('Ürün bulunamadı.');    }    const stockCardIds = (data.items ?? []).map((item) => item.stockCardId).filter(Boolean) as number[];    const stockCards = await this.prisma.stockCard.findMany({ where: { id: { in: stockCardIds } } });    if (stockCards.length !== stockCardIds.length) {      throw new BadRequestException('Reçetede geçersiz stok kartı var.');    }    await this.prisma.productRecipe.upsert({      where: { productId },      create: {        productId,        shopMarginPercent: data.shopMarginPercent ?? 30,        siteMarginPercent: data.siteMarginPercent ?? 45,        marketplaceMarginPercent: data.marketplaceMarginPercent ?? 65,      },      update: {        shopMarginPercent: data.shopMarginPercent ?? 30,        siteMarginPercent: data.siteMarginPercent ?? 45,        marketplaceMarginPercent: data.marketplaceMarginPercent ?? 65,      },    });    const recipe = await this.prisma.productRecipe.findUniqueOrThrow({ where: { productId } });    await this.prisma.$transaction([      this.prisma.recipeItem.deleteMany({ where: { recipeId: recipe.id } }),      this.prisma.recipeExtraCost.deleteMany({ where: { recipeId: recipe.id } }),      ...this.buildItemCreates(recipe.id, data),      ...this.buildExtraCostCreates(recipe.id, data),    ]);    return this.getProductRecipe(productId);  }  private buildItemCreates(recipeId: number, data: RecipePayload) {    return (data.items ?? [])      .filter((item) => item.stockCardId && item.quantity && item.unit)      .map((item) =>        this.prisma.recipeItem.create({          data: {            recipeId,            stockCardId: item.stockCardId!,            quantity: item.quantity!,            unit: item.unit!,          },        }),      );  }  private buildExtraCostCreates(recipeId: number, data: RecipePayload) {    return (data.extraCosts ?? [])      .filter((item) => item.name && item.amount !== undefined)      .map((item) =>        this.prisma.recipeExtraCost.create({          data: {            recipeId,            type: item.type ?? 'OTHER',            name: item.name!,            amount: item.amount!,          },        }),      );  }  private calculate(recipe: {    shopMarginPercent: Prisma.Decimal;    siteMarginPercent: Prisma.Decimal;    marketplaceMarginPercent: Prisma.Decimal;    items: Array<{ quantity: Prisma.Decimal; stockCard: { purchasePrice: Prisma.Decimal; packageContent: Prisma.Decimal; automaticUnitCost: Prisma.Decimal } }>;    extraCosts: Array<{ amount: Prisma.Decimal }>;  }) {    const componentTotal = recipe.items.reduce((sum, item) => {      return sum + Number(item.quantity) * this.stockUnitCost(item.stockCard);    }, 0);    const extraTotal = recipe.extraCosts.reduce((sum, item) => sum + Number(item.amount), 0);    const totalCost = componentTotal + extraTotal;    return {      componentTotal: this.round(componentTotal),      extraTotal: this.round(extraTotal),      totalCost: this.round(totalCost),      shopPrice: this.applyMargin(totalCost, Number(recipe.shopMarginPercent)),      sitePrice: this.applyMargin(totalCost, Number(recipe.siteMarginPercent)),      marketplacePrice: this.applyMargin(totalCost, Number(recipe.marketplaceMarginPercent)),      warnings: this.stockWarnings(recipe.items.map((item) => item.stockCard)),    };  }  private serializeRecipe(recipe: {    id: number;    productId: number;    shopMarginPercent: Prisma.Decimal;    siteMarginPercent: Prisma.Decimal;    marketplaceMarginPercent: Prisma.Decimal;    items: Array<{      id: number;      stockCardId: number;      quantity: Prisma.Decimal;      unit: string;      stockCard: { purchasePrice: Prisma.Decimal; packageContent: Prisma.Decimal; automaticUnitCost: Prisma.Decimal; stockQuantity: Prisma.Decimal } & Record<string, unknown>;    }>;    extraCosts: Array<{ id: number; type: RecipeCostType; name: string; amount: Prisma.Decimal }>;  }) {    return {      id: recipe.id,      productId: recipe.productId,      shopMarginPercent: Number(recipe.shopMarginPercent),      siteMarginPercent: Number(recipe.siteMarginPercent),      marketplaceMarginPercent: Number(recipe.marketplaceMarginPercent),      items: recipe.items.map((item) => ({        id: item.id,        stockCardId: item.stockCardId,        quantity: Number(item.quantity),        unit: item.unit,        stockCard: {          ...item.stockCard,          purchasePrice: Number(item.stockCard.purchasePrice),          packageContent: Number(item.stockCard.packageContent),          automaticUnitCost: Number(item.stockCard.automaticUnitCost),          stockQuantity: Number(item.stockCard.stockQuantity),        },        lineTotal: this.round(Number(item.quantity) * this.stockUnitCost(item.stockCard)),        warning: this.stockCostWarning(item.stockCard),      })),      extraCosts: recipe.extraCosts.map((item) => ({        ...item,        amount: Number(item.amount),      })),    };  }  private normalize(payload: unknown): RecipePayload {    const body = (payload ?? {}) as RecipePayload;    return {      shopMarginPercent: this.numberOrDefault(body.shopMarginPercent, 30),      siteMarginPercent: this.numberOrDefault(body.siteMarginPercent, 45),      marketplaceMarginPercent: this.numberOrDefault(body.marketplaceMarginPercent, 65),      items: (body.items ?? []).map((item) => ({        stockCardId: Number(item.stockCardId),        quantity: this.numberOrDefault(item.quantity, 0),        unit: String(item.unit ?? '').trim(),      })),      extraCosts: (body.extraCosts ?? []).map((item) => ({        type: this.validCostType(item.type),        name: String(item.name ?? '').trim(),        amount: this.numberOrDefault(item.amount, 0),      })),    };  }  private validCostType(value: unknown): RecipeCostType {    const allowed: RecipeCostType[] = ['LABOR', 'ELECTRICITY', 'SILICONE', 'PACKAGING', 'SHIPPING', 'OTHER'];    return allowed.includes(value as RecipeCostType) ? (value as RecipeCostType) : 'OTHER';  }  private numberOrDefault(value: unknown, fallback: number) {    const parsed = Number(value);    return Number.isFinite(parsed) ? parsed : fallback;  }  private applyMargin(cost: number, marginPercent: number) {    return this.round(cost * (1 + marginPercent / 100));  }  private emptyCalculation() {    return {      componentTotal: 0,      extraTotal: 0,      totalCost: 0,      shopPrice: 0,      sitePrice: 0,      marketplacePrice: 0,      warnings: [],    };  }  private stockUnitCost(stockCard: { purchasePrice: Prisma.Decimal; packageContent: Prisma.Decimal; automaticUnitCost: Prisma.Decimal }) {    const stored = Number(stockCard.automaticUnitCost);    if (stored > 0) return stored;    const packageContent = Number(stockCard.packageContent);    if (Number(stockCard.purchasePrice) > 0 && packageContent > 0) {      return Number(stockCard.purchasePrice) / packageContent;    }    return 0;  }  private stockCostWarning(stockCard: { purchasePrice: Prisma.Decimal; packageContent: Prisma.Decimal }) {    if (Number(stockCard.purchasePrice) <= 0 || Number(stockCard.packageContent) <= 0) {      return 'Stok kartında maliyet bilgisi eksik';    }    return null;  }  private stockWarnings(stockCards: Array<{ purchasePrice: Prisma.Decimal; packageContent: Prisma.Decimal }>) {    return stockCards      .filter((stockCard) => this.stockCostWarning(stockCard))      .map((stockCard) => 'Stok kartında maliyet bilgisi eksik');  }  private round(value: number) {    return Math.round(value * 100) / 100;  }}